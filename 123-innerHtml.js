// innerHtML 
const headline = document.querySelector(".headline");
console.log(headline.innerHTML);
// headline.innerHTML = "<h1>Inner html changed/bddhxgfxx </h1>";
// headline.innerHTML += "<button class= \"btn\"> Learn More </button>" 
// console.log(headline.innerHTML);

// innerHtml applies on the entire element as it gets the entire element  
// we can also assign values and change the entire element